# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/*

# Schedule scrape cache (regenerated by scripts/scrape_schedules.py)
scripts/cache/

# Mapbox walk-probe GeoJSON output (regenerated by scripts/mapbox_walk_probe.py)
scripts/mapbox_probe_output/

# wrangler runtime
.wrangler

# personal working notes (drafts, outreach prep) — not for commit
notes/

# API tokens sourced by scripts (Mapbox etc.) — never commit
.env
.env.*
!.env.example

# Wrangler dev secrets (MAPBOX_TOKEN etc.) — never commit
.dev.vars
.dev.vars.*
